specVersion: "v1beta"
serviceId: "gatsby-starter-default"
location: "us-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "workspace-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
